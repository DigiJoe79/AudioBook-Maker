# Chatterbox Multilingual Engine Dependencies
# Minimal install - let chatterbox-tts handle its own dependencies

# Core dependencies (required for engine server)
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.6.0
loguru>=0.7.0
httpx>=0.26.0

# Audio processing
scipy>=1.10.0

# Chatterbox TTS (will install all its dependencies)
chatterbox-tts==0.1.4
