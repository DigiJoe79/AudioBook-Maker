# ============================================================================
# Backend Core Dependencies (XTTS-free)
# ============================================================================
# After Engine Server Architecture migration, TTS engines run in separate
# VENVs. The core backend no longer needs XTTS/PyTorch dependencies.
#
# Setup:
#   Windows: setup.bat
#   Linux/Mac: ./setup.sh
# ============================================================================

# Web Framework
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9
sse-starlette==1.8.2

# Text Processing
spacy==3.8.7  # Latest stable version with Pydantic 2.x support
# Note: Language models installed via setup script:
#   python -m spacy download de_core_news_sm
#   python -m spacy download en_core_web_sm

# Audio Processing (for export/merging)
pydub==0.25.1
ffmpeg-python==0.2.0

# Utilities
loguru==0.7.2
pydantic==2.10.6  # Latest stable, compatible with spaCy 3.8.7
httpx==0.26.0  # HTTP client for engine server communication
pyyaml==6.0.1  # YAML parsing for engine.yaml files

# Database
# SQLite is built-in to Python

# ============================================================================
# Removed Dependencies (now in engine-specific VENVs):
# ============================================================================
# - torch, torchaudio → engines/xtts/requirements.txt
# - TTS → engines/xtts/requirements.txt
# - transformers → engines/xtts/requirements.txt
#
# Removed (not used):
# - faster-whisper, av, soundfile, noisereduce, pedalboard, python-dotenv
# ============================================================================
